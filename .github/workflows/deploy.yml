name: Deploy Hugo Site on Push

on:
  push:
    branches:
      - master  # Trigger on updates to the master branch
  workflow_dispatch: # Allows manual triggering of the workflow

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v2
        with:
          hugo-version: 'latest'

      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14'

      - name: Install npm dependencies
        run: npm install

      - name: Install PostCSS
        run: npm install postcss-cli

      - name: Build
        run: hugo --minify

      - name: Deploy to Hosting Server via SFTP
        uses: SamKirkland/FTP-Deploy-Action@4.2.0
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./public/
          server-dir: ${{ secrets.FTP_TARGET_DIR }}
          port: ${{ secrets.FTP_PORT }}

			-	name: Correct file location 
				run: mv /home/evanrowc/evanrow.com/evanrowbotham/home/evanrowc/public_html/* /home/evanrowc/public_html